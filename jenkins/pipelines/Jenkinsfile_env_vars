pipeline {
  agent {
    label 'elk'
  }
  load './jenkins/files/env_vars'
  environment {}
  stages {
    stage('Echo env_vars') {
      steps {
        sh 'for i in "${strings[@]}"; do echo "$i"; done'
      }
    }
  }
  post {
    always {
      cleanWs()
    }
  }
}
